package betterFactory.content.blocks;

import betterFactory.api.IHasInit;
import mindustry.world.Block;

public class BlocksLarge implements IHasInit {
    public static Block
            // crafting
            largeGraphitePressFactory, largeSiliconSmelterFactory, largePyratiteFactory, largeKilnFactory, largeSporePressFactory,
            largePulverizerFactory, largeMelterFactory, largeBlastFactory, largeSeparatorFactory, largeCoalCentrifugeFactory,
            largePlastaniumFactory, largeCryoffluidFactory, largePhaseFactory, largeSurgeFactory,
    // production
    largeCultivatorFactory;

    @Override
    public void init() {
//        largePressFactory = new GenericCrafter("large-graphite-press") {{
//
//        }};
    }
}
