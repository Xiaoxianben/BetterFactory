package BetterFactory.content.Init;

import mindustry.content.TechTree.TechNode;

public class ModTechNode {

    public static TechNode 晶体节点;

    public static void init() {
        /*
        // 创建 晶体 节点
        晶体节点 = TechTree.nodeProduce(ModItems.晶体, () ->
                {
                }
        );
        TechNode siliconNode = TechTree.all.find(node -> Objects.equals(node.content.name, Items.silicon.name));
        siliconNode.requiresUnlock = true;
        siliconNode.children.add(晶体节点);
        */
    }

}
